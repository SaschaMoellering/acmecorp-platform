Project-specific Codex notes:

- Always read AGENTS.md first.
- Use the canonical docs in `docs/`, `infra/`, `scripts/`, and `bench/` for facts.
- Treat `.codex/steering/` as compatibility notes, not the primary source of truth.
- For benchmark-related tasks, validate methodology and raw data before editing docs or diagrams.
- Prefer minimal, reviewable changes.
