apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: acmecorp
  labels:
    app: gateway-service
spec:
  type: LoadBalancer
  selector:
    app: gateway-service
  ports:
    - port: 80
      targetPort: 8080
      name: http
